// ratingReview.js

// Define an object representing the default ratings for a review.
const ratingReview = {
    effort: 0,
    communication: 0,
    participation: 0,
    meetingAttendance: 0,
  };

/**
* Export the ratingReview object to make it available for other modules.
* This object contains default ratings for various aspects of a review.
*/

module.exports = ratingReview;
